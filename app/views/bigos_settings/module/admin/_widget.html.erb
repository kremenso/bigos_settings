  <% settings.each do |setting| %>
    <%= form_tag  '/bigos_settings/admin/settings', :remote => true do %>
    <%= label_tag setting.first %>
    <%= text_field_tag "setting[#{setting.first}]", setting.second %>
    <%= submit_tag %>
  <% end %>
<% end %>